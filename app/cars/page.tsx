
// import { db } from '../../src/index';
// import { CarsTable } from '../../src/db/schema';

// export default async function CarsPage() {
//   const cars = await db.select().from(CarsTable);

//   return (
//     <main>
//       <h1>Lista de Carros</h1>
//       <ul>
//         {cars.map(car => (
//           <li key={car.id}>
//             {car.name} - {car.brand} - {car.year} - {car.engine}
//           </li>
//         ))}
//       </ul>
//     </main>
//   );
// }
